<h1>Editing user</h1>
<%= form_for @user, :url => users_path, :html => { :multipart => true } do |form| %>
 
<% end %>

<%= render 'form' %>

<%= link_to 'Show', @user %> |
<%= link_to 'Back', users_path %>
